# Quiet Signal — Handover (2026-07-18)

This is a handover for a fresh chat. It carries forward from the 2026-07-15 handover and records everything done in the 2026-07-18 session. **There is one active blocker at the bottom — read "What's left to do" first.**

## What Quiet Signal is
A React Native / Expo (SDK 54) Android/iOS app. One-line pitch: a 15-second daily check-in that quietly tells the people who matter how a patient is really doing. Not a diagnostic/crisis tool; rule-based (no AI vendor); row-level security throughout; running cost ~£0/month.

## Key identifiers (unchanged from 2026-07-15 unless noted)
- App package: `com.quietsignal.app`
- GitHub repo: `Valentigo-Ai/quiet-signal` (branch `main`) — local path `C:\dev\quiet-signal`
- Play Console developer account: "Cloud Seaker", account ID `7440677865943147994`, login index **u/2**, Google account `zargona2z@gmail.com`
- Play Console app ID: `4973719539259427979`
- RevenueCat: project `4d31fc1f`; app "Quiet Signal (Play Store)" (`appf15b73ddc3`)
- RevenueCat Android SDK key (public): `goog_eYaKdWiLorsmEUMdUeGLxKJaQrR`
- RevenueCat service account: `revenuecat-play-access@quiet-signal-501310.iam.gserviceaccount.com`
- Pub/Sub topic (RTDN): `projects/quiet-signal-501310/topics/Play-Store-Notifications` — note the Topic ID field in RevenueCat's UI visually truncates this with no ellipsis; always verify the real name in the Cloud Pub/Sub console, not by reading RevenueCat's display field.
- Subscriptions: `quiet_signal_pro_monthly` and `quiet_signal_pro_yearly`, both **ACTIVE**

## What was done in the 2026-07-18 session

### 1. Fixed "Purchase didn't go through — that plan isn't available" error
Root cause: the RevenueCat product catalog was completely empty (Products/Entitlements/Offerings all showed zero items), contradicting earlier handover notes. Fixed by importing both Play Store products into RevenueCat, creating a `pro` entitlement, attaching both products to it, creating a `default` offering with `$rc_monthly`/`$rc_annual` packages, and setting it as the current offering.

### 2. Fixed Real-time Developer Notifications (RTDN)
RevenueCat's Google service account was missing Cloud Pub/Sub API access. Fixed by enabling the Pub/Sub API on the GCP project and granting the service account "Pub/Sub Editor" + "Monitoring Viewer" at project level. Also hit a truncated-topic-name gotcha (see above) — resolved by reading the real topic name from the GCP Pub/Sub console directly and re-entering it in Play Console. Also granted `google-play-developer-notifications@system.gserviceaccount.com` the Publisher role on the topic itself (RevenueCat's documented requirement). Verified working: "Send test notification" succeeded, RevenueCat showed a received timestamp.

### 3. UK subscription pricing corrected for VAT
Google Play was charging UK customers £4.79/mo and £35.99/yr — a 20% VAT top-up on top of a £3.99/£29.99 base price the app's code already assumed. Richard wanted the **final, VAT-inclusive, customer-facing price** to be exactly £3.99/mo and £29.99/yr (Google is the "deemed supplier" for UK/EU digital VAT regardless of Richard's own VAT registration status — he's not registered, below the £90k threshold, no customers yet). Fixed by editing the UK row directly in Play Console's regional price table for both base plans (the "Price" column there is the final tax-inclusive price the customer pays — editing it doesn't add tax on top, it just sets that number).

### 4. Same VAT-correction applied to the other 5 countries the app has custom pricing for
Richard asked to extend the same fix to every country, then agreed to scope it down to just the 5 regions the app's `constants/proPricing.ts` already has hand-picked "charm" pricing for (everywhere else — 100+ Play Store regions — was left alone; there's no known-correct target price for those and guessing would be worse than leaving Google's auto-conversion as-is). Final live Play Console prices, both base plans:

| Country | Monthly | Yearly | Discount badge shown in-app |
|---|---|---|---|
| UK | £3.99 | £29.99 | Save 37% |
| Ireland | €4.99 | €35 | Save 42% |
| US | $4.99 | $38 | Save 37% |
| Canada | CA$6.99 | CA$52 | Save 38% |
| Australia | A$7.99 | A$60 | Save 37% |
| New Zealand | NZ$8.49 | NZ$65 | Save 36% |

All verified saved in Play Console ("Your changes have been saved" on both base plans). These match the hardcoded `savePercent` values in `proPricing.ts` exactly, so the in-app "Save X%" badges are accurate. Sanity-checked against live GBP exchange rates — none of these are meaningfully overpriced vs. a strict £-conversion (US/CA/NZ actually work out a bit cheaper; Ireland/Australia run ~5-8% higher, which is normal charm-pricing variance, not a problem). Also benchmarked against Calm/Headspace ($13-15/mo, $70/yr) — Quiet Signal's pricing is well below the market leaders, appropriate for a new, unproven app.

### 5. Fixed a stray display bug
`src/constants/proPricing.ts` — GB yearly fallback price string was literally `"£30"` (pre-existing typo, unrelated to the VAT work). Corrected to `"£29.99"` to match the real price and the yearly-savings math.

### 6. Committed and pushed the RevenueCat integration (finally)
The RevenueCat code changes from the 2026-07-15 session had been sitting uncommitted, blocked by a `.git/index.lock` file that couldn't be deleted from the assistant's sandbox (a recurring virtiofs/9p mount-bridge quirk — `rm`, `mv`, and Python `os.remove` all fail with `Operation not permitted` even though ownership/permissions look fine). Worked around it this time by committing via low-level git plumbing (`GIT_INDEX_FILE` pointed at a temp file, `write-tree` + `commit-tree` + `update-ref`, bypassing the stuck lock entirely), then asked Richard to delete the two stray `.lock` files from PowerShell (`Remove-Item .git\index.lock -Force` / `.git\HEAD.lock -Force`) — that worked fine from Windows directly. Richard then ran `git push` himself successfully.

**Commit `bffe37b3`** is now on `origin/main` (was `cd507e50`). Contains: `react-native-purchases` + RevenueCat Android key config, the rewritten `ProContext.tsx` (live entitlement/purchases), `UpgradeScreen.tsx` (live store pricing), the `proPricing.ts` £30→£29.99 fix, and both the 2026-07-12 and 2026-07-15 handover docs. Excluded (intentionally, as before): `project-notes/powershell..txt` (pre-existing line-ending-only diff — keep excluding this from every future commit) and `build-1784106710887.aab` (78MB build artifact, untracked, harmless to leave in the folder or delete).

## What's left to do

### Blocker — DO NOT test-purchase yet
Richard tried a real purchase and the Google Play checkout sheet still showed the **old** £4.79/mo price (screenshot confirmed, ~1-2 hours after the Play Console price edit was saved). Play Console itself was re-verified and definitely shows £3.99 saved — this is Google's own billing-cache propagation lag, not a mistake on our end or in the code. Google's documentation says subscription price changes "may take several hours" to propagate to the actual checkout/billing backend, separate from how fast Play Console's own UI updates.

**Richard was told explicitly not to tap Subscribe** while the checkout sheet shows the wrong price, since tapping Pay would charge the price shown on screen, not the intended one. He's agreed to wait and not attempt a purchase until it's fixed.

**Next action for whoever picks this up:** ask Richard to reopen Settings → Upgrade to Pro on his phone and check what price the paywall/checkout now shows. If it's £3.99/£29.99, it's safe to do the real purchase test (should be a no-charge Google test-purchase sheet since his account is on the license-testers list with `RESPOND_NORMALLY`). If it's still £4.79/£35.99, it just needs more time — check again later, don't chase it further technically, there's nothing left to fix code- or config-side.

### Once pricing shows correctly and purchase is tested
- Confirm Pro entitlement actually unlocks in-app after a successful purchase (unlimited recipients, 60/90-day history + PDF report, all backgrounds).
- Confirm `restorePurchases` works too, if easy to check.

### Not started (unchanged from 2026-07-15)
- Store listing still Draft — needs `support@quietsignal.co.uk` set up in Hostinger hPanel, then description/screenshots/categorization.
- RevenueCat→Supabase webhook — deferred, only needed for server-side Pro enforcement.
- Closed testing (required before production access).
- Clinical pilot (DCB0129/DCB0160, DTAC, DPIA/IG, security review, crisis-wordlist clinical review) — not started.

## Constraints / gotchas to remember (carried forward + new)
- Play Console dev account is under login index **u/2**. Use `/u/2/` URLs.
- No EAS cloud builds (out of credits). Local builds only, must run in **WSL2**, not PowerShell.
- Do NOT run `npm audit fix --force`.
- Keep `project-notes/powershell..txt` OUT of every commit (pre-existing line-ending noise, not real changes).
- **New:** if `.git/index.lock` gets stuck and can't be deleted from the assistant's sandbox, it CAN be deleted fine directly from Windows PowerShell (`Remove-Item .git\index.lock -Force`) — ask Richard to do that rather than fighting it from the sandbox side. If a commit is needed before that's possible, the `GIT_INDEX_FILE` + `write-tree`/`commit-tree`/`update-ref` plumbing workaround (used this session) works around it entirely.
- **New:** RevenueCat's Topic ID display field in its dashboard can silently truncate long Pub/Sub topic names with no visual ellipsis — always cross-check the real name in the Google Cloud Pub/Sub console before pasting it anywhere.
- **New:** editing a price in Play Console's regional price table sets the final, tax-inclusive, customer-facing price directly — it does not get VAT added on top afterward. The adjacent "Tax" column is informational only.
- **New:** Play Console price edits can take hours to propagate to the actual on-device Play Billing checkout sheet, even though Play Console's own UI reflects the change immediately. Don't trust the checkout sheet's displayed price as confirmation that a Play Console edit failed — check Play Console itself first.

## Where everything is saved
- GitHub `origin/main`: **up to date**, commit `bffe37b3` pushed this session.
- Play Console: both subscriptions active; UK/IE/US/CA/AU/NZ pricing corrected on both base plans (saved, confirmed); RTDN/Pub-Sub connected and verified working.
- RevenueCat: product catalog populated, `pro` entitlement created, `default` offering live with both packages.
- Live Supabase: unchanged this session.
