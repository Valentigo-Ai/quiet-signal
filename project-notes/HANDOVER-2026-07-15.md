# Quiet Signal — Handover (2026-07-15)

This is a handover for a fresh chat. It carries forward from the 2026-07-12 handover and records everything done in the 2026-07-15 session. Read the "IMPORTANT CORRECTIONS" section first — one key account fact from the old handover was wrong.

## What Quiet Signal is
A React Native / Expo (SDK 54) Android/iOS app. One-line pitch: a 15-second daily check-in that quietly tells the people who matter how a patient is really doing. Not a diagnostic/crisis tool; rule-based (no AI vendor); row-level security throughout; running cost ~£0/month.

## IMPORTANT CORRECTIONS to the old handover
- **The Play Console developer account "Cloud Seaker" is owned by `zargona2z@gmail.com`, NOT `cunninghamr76@gmail.com`.** Verified via the Play Console account switcher on 2026-07-15. `cunninghamr76@gmail.com` was only the original tester email. `zargona2z@gmail.com` is both the developer/owner account AND Richard's phone Play Store account.
- The developer account is still under Google **login index u/2** — the bare `play.google.com/console` URL bounces to a u/0 signup page. Always use `/u/2/` URLs.

## Key identifiers
- App package: `com.quietsignal.app`
- Supabase project ref: `bzczbsrtbnqiydscvnak`
- GitHub repo: `Valentigo-Ai/quiet-signal` (branch `main`)
- Local repo path: `C:\dev\quiet-signal`
- Play Console developer account: "Cloud Seaker", account ID `7440677865943147994`, login index **u/2**, Google account **`zargona2z@gmail.com`**
- Play Console app ID: `4973719539259427979`
- Internal testing track ID: `4700064166027906652`
- Tester opt-in link: `https://play.google.com/apps/internaltest/4700064166027906652`
- Payments profile ID: `4213-8267-6259` (VERIFIED)
- RevenueCat: project `4d31fc1f`; apps "Test Store" (`app9440a10262`) and "Quiet Signal (Play Store)" (`appf15b73ddc3`)
- RevenueCat Android SDK key (public, safe to commit): `goog_eYaKdWiLorsmEUMdUeGLxKJaQrR`
- Website: `quietsignal.co.uk`
- Subscriptions: `quiet_signal_pro_monthly` (£3.99/mo) and `quiet_signal_pro_yearly` (£29.99/yr) — both **ACTIVE**

## Architecture — three separate places (mental model)
1. App code (React Native) → reaches devices only via: edit → local build (`.aab`) → manual upload to Play Console → release. Nothing automatic.
2. Backend (Supabase edge functions + DB) → live instantly when deployed; independent of Play Console/builds.
3. Play Console (listing, testers, subscriptions, pricing) → edited in the browser; unrelated to the code.

Supabase edge functions deployed (unchanged this session): `generate-message`, `share-checkin`, `get-shared-message`, `nightly-journal-scan`, `delete-account`, `checkin-archive-scan`.

## What was done in the 2026-07-15 session

### 1. Payments profile verified
Confirmed Google's £0.30 test deposit via Play Console → Settings → Payments profile → "Verify". Bank account is now verified (Google emailed confirmation). Payments profile ID `4213-8267-6259`. This cleared the first subscription gate.

### 2. RevenueCat integrated in code (NOT yet committed)
- Added `react-native-purchases@10.4.2` to `package.json` (autolinks — no Expo config-plugin entry needed; pulls in the Play Billing Library).
- `eas.json`: added `EXPO_PUBLIC_REVENUECAT_ANDROID_KEY = goog_eYaKdWiLorsmEUMdUeGLxKJaQrR` to BOTH the `preview` and `production` env blocks.
- Rewrote `src/context/ProContext.tsx`: replaced the AsyncStorage placeholder with real RevenueCat — `Purchases.configure`, live `pro` entitlement check, `addCustomerInfoUpdateListener`, offerings preload, `purchasePackage`, `restorePurchases`. Same public context API, so no gating screen needed changes. Web/Expo Go are guarded; a failed init never crashes startup. The Settings dev toggle (`_devSetPro`) is dev-only (`__DEV__`). Entitlement id constant `ENTITLEMENT_ID = "pro"`. Plans map to packages by `PACKAGE_TYPE.MONTHLY` / `PACKAGE_TYPE.ANNUAL` (fallback: product id contains `quiet_signal_pro_monthly` / `_yearly`).
- `src/screens/pro/UpgradeScreen.tsx`: shows live store prices (`livePrices`) with fallback to `constants/proPricing.ts`; only celebrates on a genuinely completed purchase; shows an alert on purchase error. `purchasePro` now returns `Promise<boolean>`.
- `npm run typecheck` passes (verified in-session).
- Ran `npx expo install react-native-purchases` on the machine (lockfile updated; "changed 4 packages").
- Entitlement-sync decision (Richard asked for advice): go **client-side via the RevenueCat SDK** for launch (what's implemented). A RevenueCat→Supabase webhook is deferred until server-side enforcement is actually needed; the code leaves a clean seam.

### 3. Local production build
Built locally via WSL2: `cd /mnt/c/dev/quiet-signal && eas build --platform android --profile production --local`. (Running it in PowerShell fails: "Unsupported platform, macOS or Linux is required" — Android local builds must run in WSL.) Succeeded → `C:\dev\quiet-signal\build-1784106710887.aab` (78.4 MB), version code **3**, versionName 0.1.0. This build contains Play Billing + the earlier phone-number auto-format fix.

### 4. Uploaded + published to Internal testing
Created a new Internal testing release, uploaded `build-1784106710887.aab` (dragged in from File Explorer — the browser upload tool is capped at 10 MB so the 78 MB bundle can't be pushed programmatically), added release notes, and published. Track now shows **Latest release: 3 (0.1.0), Active, Available to internal testers** (released Jul 15 10:33 AM). Only benign warning: no deobfuscation/ProGuard mapping file (normal for Expo).

### 5. Created + activated both subscription products
- `quiet_signal_pro_monthly` — name "Quiet Signal Pro (Monthly)", base plan id `monthly`, Monthly auto-renewing, grace 7 days, price set from £3.99 GBP (auto-converted to 177 regions). **ACTIVE.**
- `quiet_signal_pro_yearly` — name "Quiet Signal Pro (Yearly)", base plan id `yearly`, Yearly auto-renewing, grace 14 days, price set from £29.99 GBP (auto-converted). **ACTIVE.**
- Product IDs are permanent and match the code / `constants/proPricing.ts`.

### 6. Testers / license testing
- The email list **"Internal testers"** is applied to BOTH the internal testing track AND license testing (one list, both jobs). It now has **2 members**: `cunninghamr76@gmail.com` and `zargona2z@gmail.com` (added this session). License response: `RESPOND_NORMALLY` (test purchases run through real billing but never charge).
- GOTCHA: when adding an email to a Play Console email list, you must press **Enter** to commit it as a chip BEFORE clicking Save, or it silently doesn't save (this bit us once).

### 7. Install on device
Richard opted in via the tester link on his phone (signed in as `zargona2z@gmail.com`) and the app is downloaded/installing. (A 404 on the opt-in link right after adding a tester is normal — propagation delay and/or wrong browser account; it resolved.)

## What's left to do

### Immediate
- **TEST THE PURCHASE (in progress).** In the app: Settings → Upgrade to Pro → buy. Expect a no-charge Google test-purchase sheet, the paywall showing live prices £3.99 / £29.99, and Pro unlocking (unlimited recipients, 60/90-day history + PDF, all backgrounds). If a purchase errors with "item not available", that's product propagation — wait and retry.
- **COMMIT THE CODE.** The RevenueCat work is NOT committed yet. Files changed: `package.json`, `eas.json`, `src/context/ProContext.tsx`, `src/screens/pro/UpgradeScreen.tsx`. EXCLUDE the pre-existing line-ending-only change on `project-notes/powershell..txt`. (This new handover file is also untracked and can be committed.)
- **Verify RevenueCat dashboard config** matches the code assumptions: entitlement named exactly `pro`, and the **current** Offering has monthly + annual packages built on `quiet_signal_pro_monthly` / `quiet_signal_pro_yearly`. If different, adjust `ENTITLEMENT_ID` / package matching in `ProContext.tsx`.

### Not started
- **Store listing** — still Draft ("3 of 11 complete"). Needs `support@quietsignal.co.uk` set up in Hostinger hPanel first (stores reject placeholder support addresses), then description, screenshots, categorization. Draft content in `docs/store-listing.md` / `docs/website-content.md`. Only required to go beyond internal testing.
- **RevenueCat→Supabase webhook** — deferred; only needed for server-side Pro enforcement or DB/analytics visibility.
- **Closed testing** — Google requires a closed test before you can apply for production access.
- **Clinical pilot** — DCB0129/DCB0160 safety case, DTAC, DPIA/IG sign-off, independent security review, crisis-wordlist clinical review. Not started.

## Constraints / gotchas to remember
- Play Console dev account is under login index **u/2**, Google account **`zargona2z@gmail.com`**. Use `/u/2/` URLs.
- **No EAS cloud builds** (out of credits, can't afford Pro). Local only: `eas build --platform android --profile production --local`. Must run inside **WSL2** (`cd /mnt/c/dev/quiet-signal`), NOT PowerShell (Windows errors "Unsupported platform"). `--local` uses no build credits.
- `.wslconfig` has a memory bump (10GB) to stop the Linux OOM killer aborting builds; run `wsl --shutdown` after editing it (not needed unless you edit it — the current build worked fine).
- Play Store needs `.aab` (production profile), not `.apk`.
- Do NOT run `npm audit fix --force` — the 13 moderate advisories are transitive Expo/RN warnings; the force fix breaks Expo builds.
- Pre-existing line-ending-only change on `project-notes/powershell..txt` — keep it OUT of commits.
- RevenueCat Android key lives in `eas.json` env (used by builds). It is NOT in the local `.env`, so `expo start` dev sessions have purchases disabled (they fall back to the `__DEV__` Pro toggle) — that's expected. Add `EXPO_PUBLIC_REVENUECAT_ANDROID_KEY=goog_eYaKdWiLorsmEUMdUeGLxKJaQrR` to `.env` only if you want live purchases under `expo start`.
- Adding tester emails in Play Console: press Enter to commit before Save.
- Fresh tester/product changes take minutes to a few hours to propagate; 404 / "item not available" right after a change is normal.
- The 78 MB `.aab` is too big for the assisted browser upload tool (10 MB cap) — upload it by hand (drag from File Explorer or the file picker).

## Where everything is saved
- GitHub `origin/main`: unchanged this session (code changes are LOCAL, uncommitted).
- Live Supabase: unchanged this session.
- Play Console: release 3 (0.1.0) live on internal testing; both subscriptions active; `zargona2z@gmail.com` added as tester + license tester; payments profile verified.
